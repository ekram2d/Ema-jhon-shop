import React from 'react';

const Cards = ({product}) => {
      console.log(product);
      return (
            <div>
                  <p>card</p>
            </div>
      );
};

export default Cards;